require 'spec_helper'

describe IgnoreColumns do
  it 'has a version number' do
    expect(IgnoreColumns::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
